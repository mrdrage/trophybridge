# Supabase workspace

Database migrations will begin in **M1 · Domain Model** under `supabase/migrations/`.

M0 intentionally does not create production tables or require a connected Supabase project. SQL migrations will become the source of truth for the TrophyBridge persistence model once M1 starts.
